# DO NOT COMMIT with real values.
# Use GCP Secret Manager or Sealed Secrets in production.
# To create manually:
#   kubectl create secret generic bot-street-secrets \
#     --from-literal=MONGO_URI='mongodb+srv://...' \
#     -n bot-street
apiVersion: v1
kind: Secret
metadata:
  name: bot-street-secrets
  namespace: bot-street
type: Opaque
data:
  # base64-encoded placeholder — replace before deploying
  MONGO_URI: placeholder